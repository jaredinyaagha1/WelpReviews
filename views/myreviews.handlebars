<div class="row mt-4 review-card">
  <div class="col-md-6">
    <h3>My Reviews</h3>
    {{#each reviews as |review|}}
      <div class="new-review-card">
          <img class="new-review-card-img-top" src="{{review.volumeInfo.imageLinks.thumbnail}}" alt="Card image cap">
        <div class="new-review-card-header">
          {{review.date_created}}
        </div>
        <div class="new-review-card-body">
          <h5 class="new-review-card-title">{{review.title}}</h5>
          <p class="new-review-card-text">{{review.description}}</p>
        </div>
      </div>
    {{/each}}
  </div>
</div>